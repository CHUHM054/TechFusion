# 题目

<!-- 请用自然语言描述题目条件，例如：已知序列/信号、参数、约束等。 -->
<!-- 数学公式统一用 KaTeX：行内 $...$，块级 $$...$$。 -->

设：$x(n) = \{\cdots\}$
$h(n) = \{\cdots\}$
求：$\cdots$

# 待求目标

<!-- 明确要求考生求什么，建议用条目列出。 -->

1. 目标 A（如：求 $y(n)$ 的表达式或数值）。
2. 目标 B（如：用指定方法验证结果）。
3. 目标 C（如：指出关键参数或中间量）。

# 符号表

<!-- 请用表格列出解题过程中会用到的符号，便于后续生成题目时统一引用。 -->
<!-- 每个符号必须给出可被 Streamlit/KaTeX 渲染的 formula，使用 $...$ 包裹。 -->

| 符号 | 中文含义 | 类型 | KaTeX formula |
|------|----------|------|---------------|
| $L$ | 输入序列总长度 | scalar | $L = 60$ |
| $M$ | 冲激响应长度 | scalar | $M = 3$ |
| $N$ | 分段长度 | scalar | $N = 5$ |
| $x(n)$ | 输入序列 | sequence | $x(n) = \{\cdots\}_{[0, L-1]}$ |
| $h(n)$ | 冲激响应 | sequence | $h(n) = \{\cdots\}_{[0, M-1]}$ |
| $x_i(n)$ | 第 $i$ 段输入 | sequence | $x_i(n)$ |
| $y_i(n)$ | 第 $i$ 段输出 | sequence | $y_i(n)$ |
| $i$ | 段索引 | index_range | $i = 0, 1, \dots$ |
| $P$ | DFT/FFT 点数 | scalar | $P = N + M - 1$ |

类型说明：

- `scalar`：单个数值。
- `sequence`：离散序列，用列表或带下标范围的形式表示。
- `index_range`：整数索引范围。
- `dict`：键值对集合，用于存储分段结果等结构化数据。

# Step 列表

<!-- 每个步骤用一个 `### StepN：标题` 描述，写出详细计算/推导流程。 -->

### Step1：确定参数和分段

列出关键参数并说明分段规则：
$$
L = \cdots, \quad M = \cdots, \quad N = \cdots
$$
分段方式：
$$
x_i(n) = \cdots, \quad i = 0, 1, \dots
$$

### Step2：计算中间结果

给出本步骤需要使用的公式、逐项计算过程和中间结果：
$$
y_i(n) = \sum_{k} x_i(k) h(n - k)
$$
示例逐项计算：

- $n = 0$：$\cdots$
- $n = 1$：$\cdots$
- ……

最终得到：
$$
y_i(n) = \{\cdots\}
$$

### Step3：合并/拼接最终结果

说明如何根据题目所要求的方法（如重叠相加法、重叠保留法等）合并各段结果，给出最终输出：
$$
y(n) = \{\cdots\}
$$

# 挖空建议

<!-- 标注希望考生填写的位置。类型可以是 `单值`、`列表`、`字典`。 -->
<!-- 列表变量示例：$x_i(n) = \{_,_,_,_,_,_,_\}$，序号从 $_$ 到 $_$。 -->
<!-- 字典变量示例：$X(k) =$ {实部: _, 虚部: _}。 -->

- **单值**：$L =$ `____`（输入序列总长度）。
- **单值**：$P = N + M - 1 =$ `____`（DFT/FFT 点数）。
- **列表**：第 0 段线性卷积结果 $y_0(n) = \{$ `____` $\}$。
- **列表**：中间段有效输出 $y_{eff,i}(n) = \{$ `____` $\}$。
- **字典**：各段起始索引与对应结果的映射 `{起始索引: 结果序列}`。

# 参考答案

<!-- 给出标准答案或关键结果，方便校验。 -->

$$
y(n) = \{\cdots\}
$$

关键中间量：

- $\cdots$
- $\cdots$
